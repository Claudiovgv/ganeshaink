# Dependencies
node_modules/

# Environment variables (all variants)
.env*
!.env.example

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*

# OS artefacts
.DS_Store
Thumbs.db

# IDE / editor
.vscode/
.idea/
*.swp

# Test coverage
coverage/
.nyc_output/

# Upload storage
uploads/

# Local-only seed scripts with real personal data (credentials, client PII) — never commit
prisma/seed-team.js
prisma/import-wp-bookings.js

# Prisma
node_modules/.prisma/
prisma/.env
# Keep generated client committed (pre-built — server cannot run prisma generate)
!src/generated/
# ...except the macOS dev engine binary — the server is Linux and only needs the debian-* ones
src/generated/client/query-engine-darwin*
