---
deployment:
  tasks:
    # Backend — Prisma client is pre-generated locally, NEVER run prisma generate on server
    - export DEPLOYPATH=$HOME/ganeshaink/backend
    - cd $DEPLOYPATH && npm ci --ignore-scripts
